void MDeferredRenderWork::DrawNormalMesh(MRenderInfo& info)
{
    for (auto& materialGroup : info.vMaterialRenderGroup)
    {
        // Perform rendering operations for each group
        // ...
    }
}
