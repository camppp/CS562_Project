void reset() {
    int n = 10; // size of the array
    int INF = 1000000; // large value
    int dis[n+10] = {INF, INF, INF, INF, INF, INF, INF, INF, INF, INF};
    vector<int> edge;
    vector<int> cost;
    edge.clear();
    cost.clear();
}
