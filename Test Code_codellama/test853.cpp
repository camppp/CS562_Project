void Tile::renderEditor(Buffer& buffer, float x, float y, float z) {
    // Call the render method to render the tile in the game world
    render(buffer, x, y, z);

    // Perform additional editor-specific rendering operations
    // ...
}
