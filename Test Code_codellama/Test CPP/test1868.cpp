#include <iostream>
using namespace std;

int main() {
  bool magic = true; // Define the variable 'magic' and assign a value to it

  if (magic) {
    cout << "magic" << endl; // Modify the code snippet to output "magic" if the condition is true
  } else {
    cout << "not magic" << endl; // Modify the code snippet to output "not magic" if the condition is false
  }

  return 0;
}
