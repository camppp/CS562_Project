void depthData2point(const DepthData& depth, const IntrinsicParams& params, std::vector<Point3D>& points) {
    // Implement the logic to convert depth data to 3D points using the intrinsic parameters
    // Use the provided depth data and intrinsic parameters to calculate the 3D points
    // Populate the 'points' vector with the calculated 3D points
    // Example pseudocode:
    // for each pixel in depth data {
    //     calculate 3D point using intrinsic parameters and depth value
    //     add the calculated 3D point to the 'points' vector
    // }
}
