void onDoubleTap(Point touchPosition) {
    // Perform the action for a valid double tap gesture
    // For example, you can handle the double tap by zooming in on the touched position or triggering a specific event.
    // This could involve updating the UI, triggering an animation, or any other relevant action based on the application's requirements.
}
