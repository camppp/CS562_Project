void CMainLayout::Create(CDlgMetrics& metrics, CRect& rc) {
    // Calculate the position of m_rcLabel0 based on metrics
    m_rcLabel0 = /* calculation based on metrics */;

    // Calculate the position of m_rcOKButton based on metrics
    m_rcOKButton = /* calculation based on metrics */;

    // Calculate the position of m_rcCancelButton based on metrics
    m_rcCancelButton = /* calculation based on metrics */;

    // Calculate the position of m_rcApplyButton based on metrics
    m_rcApplyButton = /* calculation based on metrics */;

    // Update the overall rectangle based on the positions of individual items
    rc = /* calculation based on individual item positions */;
}
