void Tile::renderEditor(Buffer& buffer, float x, float y, float z) const {
    render(buffer, x, y, z);
    // Perform additional editor-specific rendering operations here
    // For example, render editor-specific UI elements or overlays
}
