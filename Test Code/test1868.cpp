#include <iostream>
using namespace std;

int main() {
  bool magic = true; // Define the variable 'magic' and assign a value to it

  cout << (magic ? "" : "not ") << "magic" << endl; // Modify the code snippet to output "magic" if the condition is true and "not magic" if the condition is false

  return 0;
}
