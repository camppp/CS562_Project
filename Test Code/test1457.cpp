#include "noerr.h"

const char* GetError() {
    // Assuming the error handling module provides a function to retrieve error information
    // For example, if the error information is stored in a global variable named "errorInfo"
    // the implementation could be as follows:
    return errorInfo;
}
