void APlayerPawnTest_Male_C::ExecuteUbergraph_PlayerPawnTest_Male(int EntryPoint)
{
    // Entry point 1: Move forward
    if (EntryPoint == 1)
    {
        // Implement logic to move the player character forward
        // Example: playerCharacter.MoveForward();
    }
    // Entry point 2: Attack
    else if (EntryPoint == 2)
    {
        // Implement logic to make the player character perform an attack
        // Example: playerCharacter.PerformAttack();
    }
    // Entry point 3: Use item
    else if (EntryPoint == 3)
    {
        // Implement logic to allow the player character to use an item
        // Example: playerCharacter.UseItem();
    }
    else
    {
        // Handle invalid entry points or provide default behavior
        // Example: playerCharacter.Idle();
    }
}
